# Findings ledger

Generated by `irus ledger`. **Do not edit this file** — edit
`findings/labels.json` and regenerate (B-R32). Rows that read UNLABELLED are
findings no human has judged yet; they are shown rather than hidden, because a
ledger that omits its unjudged rows is a ledger with a thumb on the scale.

Every run below is **unfiltered**: no baseline subtraction, no confidence gate,
no suppressions. This measures the checker, not the gate.

## Totals

- findings: **197**
- true: **0**
- false: **0**
- unlabelled: **197**


This is not a claim of zero false positives and never will be (B-R25). The false-positive count above is the point of the artifact, not a footnote to it.

## dispatch

```
repo: dispatch               findings: 134   true: 0    false: 0    unlabelled: 134
  seam-ca623772  env FORMKIT_ENTERPRISE_TOKEN read, never set         UNLABELLED
  seam-a94fe1b4  env VITE_DISPATCH_AVATAR_TEMPLATE read, never set    UNLABELLED
  seam-fe0ee05f  env VITE_DISPATCH_SENTRY_DSN read, never set         UNLABELLED
  seam-18a9e0a2  env VITE_DISPATCH_SENTRY_ENABLED read, never set     UNLABELLED
  seam-024f2fa8  env VITE_FORMKIT_PRO_PROJECT_KEY read, never set     UNLABELLED
  seam-8ef8fc84  DELETE /filters/{signal_filter_id} zero callers      UNLABELLED
  seam-aa0c905d  DELETE /instances/{plugin_instance_id} zero callers  UNLABELLED
  seam-af475272  DELETE /{alert_id} zero callers                      UNLABELLED
  seam-e5f2d8ac  DELETE /{case_cost_id} zero callers                  UNLABELLED
  seam-5225c0c6  DELETE /{case_cost_type_id} zero callers             UNLABELLED
  seam-91d92f93  DELETE /{case_id} zero callers                       UNLABELLED
  seam-0987dc28  DELETE /{case_id}/event/{event_uuid} zero callers    UNLABELLED
  seam-9f247f5f  DELETE /{case_id}/remove/{email} zero callers        UNLABELLED
  seam-1aeaddda  DELETE /{cost_model_id} zero callers                 UNLABELLED
  seam-fe1d532e  DELETE /{definition_id} zero callers                 UNLABELLED
  seam-1de932c4  DELETE /{document_id} zero callers                   UNLABELLED
  seam-bded14a4  DELETE /{email_template_id} zero callers             UNLABELLED
  seam-9d26db9e  DELETE /{entity_id} zero callers                     UNLABELLED
  seam-2f05daf7  DELETE /{entity_type_id} zero callers                UNLABELLED
  seam-744a5411  DELETE /{feedback_id} zero callers                   UNLABELLED
  seam-929263f5  DELETE /{forms_id}/{individual_contact_id} zero callers UNLABELLED
  seam-ebef62e2  DELETE /{forms_type_id}/{individual_contact_id} zero callers UNLABELLED
  seam-6bd4d0e6  DELETE /{incident_cost_id} zero callers              UNLABELLED
  seam-f0b46dbf  DELETE /{incident_cost_type_id} zero callers         UNLABELLED
  seam-3a615cbb  DELETE /{incident_id} zero callers                   UNLABELLED
  seam-c888fe0d  DELETE /{incident_id}/event/{event_uuid} zero callers UNLABELLED
  seam-202b883c  DELETE /{incident_id}/remove/{email} zero callers    UNLABELLED
  seam-a04dd988  DELETE /{individual_contact_id} zero callers         UNLABELLED
  seam-7ef789da  DELETE /{notification_id} zero callers               UNLABELLED
  seam-d646cd50  DELETE /{project_id} zero callers                    UNLABELLED
  seam-b17a6ae8  DELETE /{prompt_id} zero callers                     UNLABELLED
  seam-5e3a87a8  DELETE /{query_id} zero callers                      UNLABELLED
  seam-51d7ccd6  DELETE /{search_filter_id} zero callers              UNLABELLED
  seam-62675351  DELETE /{service_feedback_id}/{individual_contact_id} zero callers UNLABELLED
  seam-13d3d1b1  DELETE /{service_id} zero callers                    UNLABELLED
  seam-761c3638  DELETE /{signal_id} zero callers                     UNLABELLED
  seam-44b4c2ed  DELETE /{source_data_format_id} zero callers         UNLABELLED
  seam-e86df7c1  DELETE /{source_environment_id} zero callers         UNLABELLED
  seam-2c4e2d8a  DELETE /{source_id} zero callers                     UNLABELLED
  seam-a5baf41b  DELETE /{source_status_id} zero callers              UNLABELLED
  seam-8a132de9  DELETE /{source_transport_id} zero callers           UNLABELLED
  seam-3d7af9bc  DELETE /{source_type_id} zero callers                UNLABELLED
  seam-7bc61043  DELETE /{tag_id} zero callers                        UNLABELLED
  seam-7a82d896  DELETE /{tag_type_id} zero callers                   UNLABELLED
  seam-d5fc96a0  DELETE /{task_id} zero callers                       UNLABELLED
  seam-24df0bce  DELETE /{team_contact_id} zero callers               UNLABELLED
  seam-397666eb  DELETE /{term_id} zero callers                       UNLABELLED
  seam-0165953f  DELETE /{workflow_id} zero callers                   UNLABELLED
  seam-e02189be  PATCH /{case_id}/event zero callers                  UNLABELLED
  seam-1dcd9d8d  PATCH /{incident_id}/event zero callers              UNLABELLED
  seam-43f08a8f  POST /completed/{form_id} zero callers               UNLABELLED
  seam-fc817c51  POST /engagements zero callers                       UNLABELLED
  seam-d41680cc  POST /export zero callers                            UNLABELLED
  seam-7c7d5dff  POST /filters zero callers                           UNLABELLED
  seam-b4573f1a  POST /instances zero callers                         UNLABELLED
  seam-d508a3a0  POST /instances zero callers                         UNLABELLED
  seam-15fcdec9  POST /login zero callers                             UNLABELLED
  seam-a6c1f4f2  POST /mfa zero callers                               UNLABELLED
  seam-bc3d316b  POST /slack/action zero callers                      UNLABELLED
  seam-3f2e9702  POST /slack/command zero callers                     UNLABELLED
  seam-e661b3cc  POST /slack/event zero callers                       UNLABELLED
  seam-f653b8da  POST /slack/menu zero callers                        UNLABELLED
  seam-538b8b95  POST /ticket/{task_id} zero callers                  UNLABELLED
  seam-f5236bea  POST /{case_id} zero callers                         UNLABELLED
  seam-b2d546c0  POST /{case_id}/add/{email} zero callers             UNLABELLED
  seam-837e71db  POST /{case_id}/event zero callers                   UNLABELLED
  seam-556a70bf  POST /{case_id}/exportTimeline zero callers          UNLABELLED
  seam-360191cf  POST /{case_id}/join zero callers                    UNLABELLED
  seam-87100c7f  POST /{case_id}/resources zero callers               UNLABELLED
  seam-fb0a1cfb  POST /{case_id}/resources/conversation zero callers  UNLABELLED
  seam-875ba845  POST /{incident_id}/add/{email} zero callers         UNLABELLED
  seam-3ff9726b  POST /{incident_id}/event zero callers               UNLABELLED
  seam-2d53dbaf  POST /{incident_id}/exportTimeline zero callers      UNLABELLED
  seam-abcb5086  POST /{incident_id}/join zero callers                UNLABELLED
  seam-5269a86d  POST /{incident_id}/report/executive zero callers    UNLABELLED
  seam-2f5ff544  POST /{incident_id}/report/tactical zero callers     UNLABELLED
  seam-f2471088  POST /{incident_id}/resources zero callers           UNLABELLED
  seam-7aa8fc0e  POST /{incident_id}/subscribe zero callers           UNLABELLED
  seam-9275a500  POST /{user_id}/change-password zero callers         UNLABELLED
  seam-09273d20  POST /{user_id}/reset-password zero callers          UNLABELLED
  seam-6f564f8b  POST /{workflow_id}/run zero callers                 UNLABELLED
  seam-c3076b7b  PUT /engagements/{signal_engagement_id} zero callers UNLABELLED
  seam-630a605f  PUT /filters/{signal_filter_id} zero callers         UNLABELLED
  seam-0ebf1e03  PUT /instances/{plugin_instance_id} zero callers     UNLABELLED
  seam-74a7cb5f  PUT /me/settings zero callers                        UNLABELLED
  seam-398c8c01  PUT /recalculate/{entity_type_id}/{case_id} zero callers UNLABELLED
  seam-89c79714  PUT /update/{signal_id} zero callers                 UNLABELLED
  seam-c4fee68b  PUT /{alert_id} zero callers                         UNLABELLED
  seam-f8118daf  PUT /{case_cost_id} zero callers                     UNLABELLED
  seam-5e95743a  PUT /{case_cost_type_id} zero callers                UNLABELLED
  seam-24142fa0  PUT /{case_id} zero callers                          UNLABELLED
  seam-6a12b7c8  PUT /{case_id}/escalate zero callers                 UNLABELLED
  seam-93a40c2a  PUT /{case_priority_id} zero callers                 UNLABELLED
  seam-12804fb5  PUT /{case_severity_id} zero callers                 UNLABELLED
  seam-1d64b05c  PUT /{case_type_id} zero callers                     UNLABELLED
  seam-8eba92a0  PUT /{cost_model_id} zero callers                    UNLABELLED
  seam-27ae67dd  PUT /{definition_id} zero callers                    UNLABELLED
  seam-ad67460d  PUT /{document_id} zero callers                      UNLABELLED
  seam-2ee3aeae  PUT /{email_template_id} zero callers                UNLABELLED
  seam-ff965136  PUT /{entity_id} zero callers                        UNLABELLED
  seam-39d18964  PUT /{entity_type_id} zero callers                   UNLABELLED
  seam-9793c06d  PUT /{entity_type_id}/process zero callers           UNLABELLED
  seam-d1daf8ad  PUT /{feedback_id} zero callers                      UNLABELLED
  seam-303010c3  PUT /{forms_id}/{individual_contact_id} zero callers UNLABELLED
  seam-660439b6  PUT /{forms_type_id}/{individual_contact_id} zero callers UNLABELLED
  seam-a5fe546f  PUT /{incident_cost_id} zero callers                 UNLABELLED
  seam-329b7941  PUT /{incident_cost_type_id} zero callers            UNLABELLED
  seam-a57eb6fc  PUT /{incident_id} zero callers                      UNLABELLED
  seam-f94b5407  PUT /{incident_priority_id} zero callers             UNLABELLED
  seam-5e78bb0a  PUT /{incident_severity_id} zero callers             UNLABELLED
  seam-0c8a629e  PUT /{incident_type_id} zero callers                 UNLABELLED
  seam-796c8674  PUT /{individual_contact_id} zero callers            UNLABELLED
  seam-634b1441  PUT /{notification_id} zero callers                  UNLABELLED
  seam-b1dc70c7  PUT /{organization_id} zero callers                  UNLABELLED
  seam-ba058ff4  PUT /{project_id} zero callers                       UNLABELLED
  seam-21abcdf1  PUT /{prompt_id} zero callers                        UNLABELLED
  seam-3648136e  PUT /{query_id} zero callers                         UNLABELLED
  seam-28ac7142  PUT /{role} zero callers                             UNLABELLED
  seam-83189531  PUT /{search_filter_id} zero callers                 UNLABELLED
  seam-dbdca45c  PUT /{service_id} zero callers                       UNLABELLED
  seam-a28f2226  PUT /{signal_id} zero callers                        UNLABELLED
  seam-a2f86896  PUT /{source_data_format_id} zero callers            UNLABELLED
  seam-9fec2567  PUT /{source_environment_id} zero callers            UNLABELLED
  seam-7d3c75f6  PUT /{source_id} zero callers                        UNLABELLED
  seam-12dd9c94  PUT /{source_status_id} zero callers                 UNLABELLED
  seam-203ce25f  PUT /{source_transport_id} zero callers              UNLABELLED
  seam-d9c927a3  PUT /{source_type_id} zero callers                   UNLABELLED
  seam-cc5da0d3  PUT /{tag_id} zero callers                           UNLABELLED
  seam-39dd1565  PUT /{tag_type_id} zero callers                      UNLABELLED
  seam-ff73b7f2  PUT /{task_id} zero callers                          UNLABELLED
  seam-baa92758  PUT /{team_contact_id} zero callers                  UNLABELLED
  seam-23004150  PUT /{term_id} zero callers                          UNLABELLED
  seam-8816e799  PUT /{user_id} zero callers                          UNLABELLED
  seam-e8688849  PUT /{workflow_id} zero callers                      UNLABELLED
```

_876 files swept in 1586 ms at `dd2837e82a0b`._

## flower

```
repo: flower                 findings: 4     true: 0    false: 0    unlabelled: 4
  seam-bcb79537  env FLOWER_OAUTH2_KEY read, never set                UNLABELLED
  seam-c7393e2e  env FLOWER_OAUTH2_OKTA_BASE_URL read, never set      UNLABELLED
  seam-ae10c4c6  env FLOWER_OAUTH2_REDIRECT_URI read, never set       UNLABELLED
  seam-20534ea3  env FLOWER_OAUTH2_SECRET read, never set             UNLABELLED
```

_58 files swept in 231 ms at `45b810f472f7`._

## full-stack-fastapi-template

```
repo: full-stack-fastapi-template findings: 19    true: 0    false: 0    unlabelled: 19
  seam-1360e44d  env MAILCATCHER_HOST read, never set                 UNLABELLED
  seam-af576751  env PLAYWRIGHT_BASE_URL read, never set              UNLABELLED
  seam-e20d046d  DELETE /items/{id} zero callers                      UNLABELLED
  seam-b0f7efdf  DELETE /users/me zero callers                        UNLABELLED
  seam-7e91e3e8  DELETE /users/{user_id} zero callers                 UNLABELLED
  seam-1751b31a  PATCH /users/me zero callers                         UNLABELLED
  seam-eea15099  PATCH /users/me/password zero callers                UNLABELLED
  seam-d1f55202  PATCH /users/{user_id} zero callers                  UNLABELLED
  seam-5931087a  POST /items/ zero callers                            UNLABELLED
  seam-a93c72ce  POST /login/access-token zero callers                UNLABELLED
  seam-b15e4994  POST /login/test-token zero callers                  UNLABELLED
  seam-3301292c  POST /password-recovery-html-content/{email} zero callers UNLABELLED
  seam-8d5c44ab  POST /password-recovery/{email} zero callers         UNLABELLED
  seam-19a7034c  POST /private/users/ zero callers                    UNLABELLED
  seam-e79fbb94  POST /reset-password/ zero callers                   UNLABELLED
  seam-c577636b  POST /users/ zero callers                            UNLABELLED
  seam-779e640b  POST /users/signup zero callers                      UNLABELLED
  seam-c2842f91  POST /utils/test-email/ zero callers                 UNLABELLED
  seam-08493328  PUT /items/{id} zero callers                         UNLABELLED
```

_148 files swept in 119 ms at `66f444a63a11`._

## redash

```
repo: redash                 findings: 21    true: 0    false: 0    unlabelled: 21
  seam-4c5c9d36  env BUNDLE_ANALYZER read, never set                  UNLABELLED
  seam-eca80dec  env CYPRESS_ read, never set                         UNLABELLED
  seam-eb3620b5  env DATABRICKS_ROW_LIMIT read, never set             UNLABELLED
  seam-eab3276a  env DEV_SERVER_HOST read, never set                  UNLABELLED
  seam-b2e554cb  env HOT_RELOAD read, never set                       UNLABELLED
  seam-7950fb99  env REDASH_COOKIE_SECRET read, never set             UNLABELLED
  seam-18a2d122  env REDASH_HSTS_ENABLED read, never set              UNLABELLED
  seam-0a002184  env REDASH_LDAP_BIND_DN read, never set              UNLABELLED
  seam-c5fdac29  env REDASH_LDAP_URL read, never set                  UNLABELLED
  seam-88c0f4ef  env REDASH_MAIL_DEFAULT_SENDER read, never set       UNLABELLED
  seam-ca4c60c6  env REDASH_MAIL_MAX_EMAILS read, never set           UNLABELLED
  seam-1f4bafed  env REDASH_MAIL_PASSWORD read, never set             UNLABELLED
  seam-1bec2b5c  env REDASH_MAIL_USERNAME read, never set             UNLABELLED
  seam-1b4a2036  env REDASH_REMEMBER_COOKIE_SECURE read, never set    UNLABELLED
  seam-d8fa2627  env REDASH_SAML_LOCAL_METADATA_PATH read, never set  UNLABELLED
  seam-b128dffa  env REDASH_SEARCH_DN read, never set                 UNLABELLED
  seam-af2fc54c  env REDASH_SENTRY_ENVIRONMENT read, never set        UNLABELLED
  seam-530356cd  env REDASH_SESSION_COOKIE_SECURE read, never set     UNLABELLED
  seam-3bcba1a1  env REMOTE_DEBUG read, never set                     UNLABELLED
  seam-98cc8ffe  env SQLALCHEMY_MAX_OVERFLOW read, never set          UNLABELLED
  seam-e823bdb5  env SQLALCHEMY_POOL_SIZE read, never set             UNLABELLED
```

_861 files swept in 1075 ms at `19564c46381f`._

## reflex

```
repo: reflex                 findings: 19    true: 0    false: 0    unlabelled: 19
  seam-5645c793  env BLUBB read, never set                            UNLABELLED
  seam-298f1f62  env BOOLEAN read, never set                          UNLABELLED
  seam-ca9447f6  env CODESPACE_NAME read, never set                   UNLABELLED
  seam-dc12d705  env GITHUB_CODESPACES_PORT_FORWARDING_DOMAIN read, never set UNLABELLED
  seam-065d3529  env LIST read, never set                             UNLABELLED
  seam-048d9552  env LOCALAPPDATA read, never set                     UNLABELLED
  seam-1ba8c0b8  env LOOPS_API_KEY read, never set                    UNLABELLED
  seam-3ba7b80a  env RCC_ENDPOINT read, never set                     UNLABELLED
  seam-ef997629  env REACT_ROUTER_VERSION read, never set             UNLABELLED
  seam-2094f2f6  env REACT_VERSION read, never set                    UNLABELLED
  seam-4e658058  env REFLEX_BUN_PATH read, never set                  UNLABELLED
  seam-65ba1f8f  env REFLEX_EMBED_ORIGIN read, never set              UNLABELLED
  seam-a65e5a67  env REFLEX_ENV_FILE read, never set                  UNLABELLED
  seam-24003641  env REFLEX_MOUNT_TARGET read, never set              UNLABELLED
  seam-5cd313a6  env REFLEX_OVERRIDE_BACKEND_URL read, never set      UNLABELLED
  seam-9711a60a  env REFLEX_OVERRIDE_FRONTEND_URL read, never set     UNLABELLED
  seam-be33481d  env REPLIT_DEV_DOMAIN read, never set                UNLABELLED
  seam-1822b503  env TEST_VAR read, never set                         UNLABELLED
  seam-a13bb58c  env __INTERNAL read, never set                       UNLABELLED
```

_894 files swept in 2664 ms at `333b78bf2e32`._
